# Note Color Formatter (Builtin)

This plugin formats note timing division values used by color grouping logic.

The toolbar action opens a native scope dialog. It can format selected Normal/Rain
notes, notes whose start beat lies in an explicit beat range, or the entire chart.
Sound notes are not changed. The host reloads the result as one undoable action.
